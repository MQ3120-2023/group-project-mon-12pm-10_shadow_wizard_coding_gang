const Info = ({boxType}) => {

    return(
        <section id="info-box">
            <p id="info-header">Upcoming Events</p>
            <div id="info-list">
                <p>Placeholder</p>
            </div>
        </section>
    )
}

export default Info