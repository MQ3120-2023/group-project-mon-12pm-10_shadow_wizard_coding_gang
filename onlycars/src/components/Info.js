const Info = ({infoType}) => {

    return(
        <section id="info-box">
            <p id="info-header">{infoType}</p>
            <div id="info-list">
                <p>Placeholder</p>
            </div>
        </section>
    )
}

export default Info